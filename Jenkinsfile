pipeline {
    agent any
    stages {
        stage('Test Docker Access') {
            steps {
                script {
                    echo "Docker object: ${docker}"
                }
            }
        }
    }
}
